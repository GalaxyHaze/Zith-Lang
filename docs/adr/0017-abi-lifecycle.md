# ABI Lifecycle

Zith defines every externally visible contract through four states: drawing,
promising, signing, and conforming. The lifecycle keeps the idea, technical
promises, ADRs, and tests in one verifiable chain so a changed idea does not
silently leave stale promises or stale tests behind.

The source of truth is `docs/specs/abi-lifecycle.md`.

Status: accepted
