# ZithProject.toml — generated by 'zith new sample'

[project]
name = "sample"
version = "0.1.0"
description = ""
authors = ""
license = "MIT"
homepage = ""

[build]
entry = "src/main.zith"
output = "bin/sample"
mode = "debug"
edition = "2024"
opt_level = 0
debug_level = 2

[paths]
src_dir = "src"
bin_dir = "bin"
lib_dir = "lib"
docs_dir = "docs"
test_dir = "examples"
cache_dir = ".zith_cache"

# Optional: dependencies, link_libs, include_dirs, etc.
