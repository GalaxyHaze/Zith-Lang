from std/io/console;

struct Point{
    x: i32,
    y: i32,
    z: i32
}

fn getShare(): unique i32;

fn takeShare(x: unique i32){

}

fn main(){
    let data: unique i32 = getShare();
    takeShare(data);
    let p: ?Point = null;
    if (p)
    let pack = |0, 0, 0|;
    var x = 5;
    x = 5;
    println("-Hello World!");
}
