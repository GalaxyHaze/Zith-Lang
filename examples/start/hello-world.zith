from std/io/console;

struct Point{
    let [x,y,z]
}

fn getShare(): unique i32;

fn takeShare(x: share i32){

}

fn main(){
    let data: unique i32 = getShare();
    takeShare(share data);
    let[x, y, z] = |0, 0, 0|;
    x = 5;
    println("-Hello World!");
}
