import std/io/console;

pub struct Point{
    var x, y, z: unique i32
}

pub global x;

mod fn prot(): i32{
    let x;
    var x;
    const x = 0;
    
    if ( x > 0)
        return 0;
    return -1;
}

fn proto(x, y){}

const x: i32;

fn main(){
    let x: Xablau;
    prot(5);
}